import React from "react";
import RedirectHeader from "@/components/RedirectHeader";

const Page = () => {
  return (
    <div>
      <RedirectHeader name="support" />
    </div>
  );
};

export default Page;
